<div>
    <h2><%= t('.header') %> </h2>
    <%= render 'shared/errors', resource: @test %>
    <table class = "table">
      <thead>
        <th class="align-middle"><%= t('.name') %></th>
        <th class="align-middle"><%= t('.image') %></th>
        <th class="align-middle"><%= t('.rule') %></th>
        <th class="align-middle"><%= t('.point') %> </th>
        <th class="align-middle"><%= t('.actions') %> </th>
      </thead>
      <tbody class="table-hover">
        <%= render @badges %>
      </tbody>
    </table>
    <%= link_to t('.create_badge'), new_admin_badge_path %>
  </div>
