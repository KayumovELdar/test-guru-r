<tr>
  <td><%= gist.user.email %></td>
  <td><%= link_to(truncate(gist.question.text, length:25), question_path(gist.question)) %></td>
  <td><%= link_to gist_hash(gist.url), gist.url,
                  target: :_blank, rel: :nofollow %></td>
</tr>
