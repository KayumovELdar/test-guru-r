<p>
  <strong>Name</strong>
  <%= @test.name %>
</p>

<p>
  <strong>Level</strong>
  <%= @test.level %>
</p.>
<table>
  <%= render @test.questions%>
</table>
<li><%= link_to "Edit", edit_test_path(@test) %></li>
<li><%= link_to "Destroy", test_path(@test),
                method: :delete,
                data: {confirm: "Are you sure"}%></li>
<li><%= link_to 'Add a question', new_test_question_path(@test)%></li>
